export enum Message {
  ServiceStart = 'Service is running',
  ControlUse = 'LED control works',
  ControlStop = 'LED control turned off',
  FlowingUse = 'Flowing LEDs works',
  FlowingStop = 'Flowing LEDs turned off',
}

export enum Direction {
  Up = 'up',
  Down = 'down',
}
